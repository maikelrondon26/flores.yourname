window.onload = () => {
  // Eliminar la clase container para iniciar las animaciones
  const container = document.querySelector('.container');
  if (container) {
    container.classList.remove("container");
  }
};